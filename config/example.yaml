out_dir: <path/to/out_dir>
pipeline_dir: <path/to/pipeline_dir>
clone_cn_file: <path/to/clone_cn.tsv.gz>
clone_tree_newick: <path/to/clone_tree.nwk>
ctdna_file: <path/to/sample_A.tsv.gz>
# The following section is entirely optional, if omitted, default settings will be used
# The example below lists common options to modify and is populated with the defaults
num_chains: 8
num_restarts: 3
num_rounds: 12
num_threads: 4
